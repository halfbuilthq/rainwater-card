{
  "name": "rainwater-card",
  "version": "0.1.0",
  "description": "A single-reservoir rainwater storage card for Home Assistant.",
  "license": "MIT",
  "type": "module",
  "main": "dist/rainwater-card.js",
  "files": [
    "dist",
    "README.md",
    "hacs.json"
  ],
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@mdi/js": "^7.4.47",
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
